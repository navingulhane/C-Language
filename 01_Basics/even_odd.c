// EVEN / ODD (^0^)
   #include <stdio.h>

   int main() {
   int num ;
   
   printf("Entre a number :");
   scanf("%d", &num);
   
   if (num %2 == 0) {
// make sure that num/2 gives 0 remender ^_^       
   printf("%d is Even a number\n", num);
   }
   else {
   printf("%d is Odd a number\n", num);
   }

   return 0;
   }
